# GunTurret
#
# For debugging the vision system
#

Main:
	sys.velocity = store2 0 0
	ifnz sys.agents.data.dist DoFire
	sys.aim = add 7 sys.aim 
	jump Main
DoFire:
	Force = min sys.energy 50
	sys.fire = Force 
	jump Main
